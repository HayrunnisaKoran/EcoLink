@model web_backend.Models.RegisterViewModel

@{
    ViewData["Title"] = "Giriş Yap";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="login-page">
    <div class="login-card">
        <h1>EcoLink</h1>
        <p>Kampüs içi akıllı atık & kompost ekosistemi</p>

        @if (ViewBag.Error != null)
        {
            <div class="error-message">@ViewBag.Error</div>
        }

        <form method="post" asp-controller="Auth" asp-action="Login">
            <input type="email" name="email" placeholder="@@cbu.edu.tr e-posta adresiniz" required />

            <input type="password" name="password" placeholder="Şifreniz" required />

            <button type="submit">Giriş Yap</button>
        </form>

        <div class="auth-footer" style="margin-top: 15px; font-size: 0.9em;">
            <span>Henüz hesabın yok mu? <a href="/Auth/Register">Kayıt Ol</a></span>
        </div>

        <small>Demo giriş: admin@cbu.edu.tr / 123456</small>
    </div>
</div>
